/*
class Session {
  final String id;
  final bool reveal;
  Session({required this.id, this.reveal = false});

  factory Session.fromMap(String id, Map<String, dynamic> map) =>
      Session(id: id, reveal: map['reveal'] as bool? ?? false);

  Map<String, dynamic> toMap() => {'reveal': reveal};
}
*/
